# Environment Variables Template
# Copy this file to .env (DO NOT COMMIT .env to version control)

# =============================================================================
# 🔒 SECURITY WARNING
# =============================================================================
# NEVER commit real credentials to version control
# Use a password manager or secure vault for production secrets
# This file provides structure only - replace ALL values before use
# =============================================================================

# -----------------------------------------------------------------------------
# Admin Credentials (for testing only - use secure vault in production)
# -----------------------------------------------------------------------------
ADMIN_EMAIL=admin@appydex.com
ADMIN_PASSWORD=YourSecurePasswordHere  # ⚠️ REPLACE before use

# -----------------------------------------------------------------------------
# API Configuration
# -----------------------------------------------------------------------------
# Development (default in code)
# API_BASE_URL=http://localhost:16110

# Staging
# API_BASE_URL=https://api-staging.appydex.co

# Production
# API_BASE_URL=https://api.appydex.com

# -----------------------------------------------------------------------------
# Error Tracking & Observability
# -----------------------------------------------------------------------------
# Sentry DSN for error reporting
# Format: https://[key]@[org].ingest.sentry.io/[project]
SENTRY_DSN=

# Production Sentry (separate project recommended)
PROD_SENTRY_DSN=

# OpenTelemetry (if implemented)
# OTEL_ENDPOINT=
# OTEL_API_KEY=

# -----------------------------------------------------------------------------
# Payment Integration (if applicable)
# -----------------------------------------------------------------------------
# Stripe Test Keys (starts with pk_test_ / sk_test_)
# STRIPE_PUBLISHABLE_KEY=
# STRIPE_SECRET_KEY=

# Stripe Live Keys (starts with pk_live_ / sk_live_)
# STRIPE_LIVE_PUBLISHABLE_KEY=
# STRIPE_LIVE_SECRET_KEY=

# Stripe Webhook Secret (starts with whsec_)
# STRIPE_WEBHOOK_SECRET=

# -----------------------------------------------------------------------------
# Build Configuration
# -----------------------------------------------------------------------------
# APP_FLAVOR=dev|staging|prod
APP_FLAVOR=dev

# -----------------------------------------------------------------------------
# Usage Instructions
# -----------------------------------------------------------------------------
# 1. Copy this file: cp .env.example .env
# 2. Fill in required values (never commit .env)
# 3. For CI/CD: Set as repository secrets
# 4. For production: Use secret management service (AWS Secrets Manager, etc.)
# 5. For local dev: Source before running: source .env && flutter run
#
# Build with environment variables:
# flutter build web \
#   --dart-define=APP_FLAVOR=$APP_FLAVOR \
#   --dart-define=API_BASE_URL=$API_BASE_URL \
#   --dart-define=SENTRY_DSN=$SENTRY_DSN \
#   --release
